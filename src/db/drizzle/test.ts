/*import { drizzleDb } from '.';
import { postsTable } from './schemas';

(async () => {
  const posts = drizzleDb.select().from(postsTable);
  (await posts).forEach(post => {
    console.log(post.title);
  });
})();
*/
